import { FC, ReactElement } from "react";

const Footer: FC = (): ReactElement => (
  <footer className="footer flex items-center justify-center space-x-2 bg-gray-500 p-5">
    <h2>Footer</h2>
  </footer>
);

export default Footer;
